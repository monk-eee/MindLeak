- Known limitations now live in `docs/KNOWN-LIMITATIONS.md`, separate from the
  `gaps.d/` defect catalogue (ADR-0138). A gap fragment asserts that something
  in this repository could be fixed; entries that describe a deliberate design
  position, or a defect in a tool this project does not own, have no such fix
  and made the gap backlog read as twice the outstanding work it actually held.
  Eighteen entries moved with their evidence intact and nothing was deleted;
  `gaps.d/` went from 36 fragments to 18, every one of which names something
  fixable.
