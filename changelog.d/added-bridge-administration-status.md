- **Bridge now exposes tenant-scoped Administration status.** Operators can
  inspect projection health and visible lifecycle capabilities while snapshot,
  export, recovery, and purge work remain explicitly unavailable until the
  required authorization and policy basis exists.
