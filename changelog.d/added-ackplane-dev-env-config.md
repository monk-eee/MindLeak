- **Ackplane's local dev config (ports, database URL, Bridge listen address/tenant/salt path) is now documented and persisted.** Added `.env.example` (copy to a gitignored `.env`; `docker-compose.yml` already reads it natively) and `scripts/run-ackplane.mjs` (`node scripts/run-ackplane.mjs bridge|server`), a small loader so the plain `ackplane-bridge`/`ackplane-server` binaries pick up the same `.env` when run outside Compose. The Bridge's ADR-0098 per-installation salt now has a documented, gitignored, repo-conventional home (`.mindleak/ackplane-bridge-salt.bin`) instead of being regenerated ad hoc each session.
