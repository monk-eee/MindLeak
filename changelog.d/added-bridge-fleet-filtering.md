- **The Bridge Fleet screen now supports client-side filtering by freshness and coordination state, plus a per-repository detail drill-through (ADR-0095 decision 5).** `GET /api/v1/fleet` responses now include each repository's `freshness` classification (fresh/lagging/never_projected), reusing the same classification the repository-detail endpoint already computes. No new backend route was needed for drill-through: the "Detail" toggle fetches the existing `GET /api/v1/repositories/{id}` and `.../timeline` endpoints inline.
