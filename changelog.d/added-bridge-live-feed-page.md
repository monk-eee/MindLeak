- **Bridge now includes a snapshot-first Live Feed page.** Operators can load
  a repository's current state, watch bounded replayable operations events,
  and see explicit reconnect, stale, and resynchronization state without
  exposing raw source or publisher metadata.
