- **Ackplane now has a durable supervisor/session projection foundation.** It
  records tenant-scoped supervisor registrations, heartbeat freshness, worker
  sessions, and lifecycle receipts without introducing a remote command path.
