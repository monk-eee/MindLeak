- **`task_query(view="board")` takes an opt-in `detail=false` to skip its three per-row engine lookups.** `board()` (`crates/lodestar-mcp/src/tools/executive/tasks.rs`) enriched every task with a `scope`, `claim_window`, and `receipt` lookup unconditionally, which reached roughly 1.95 MB on this repository's own long-lived board. `detail=false` returns every task (nothing is dropped) but omits those three lookups plus the free-text `acceptance` field; the default stays `true` so no existing caller's contract changes. Nine callers now pass it, each verified to read only base task fields: `scripts/board-health.mjs`, `scripts/canonical-push.mjs`, `scripts/evaluate-pr-effectiveness.mjs`, `scripts/status.mjs`, `scripts/worktree-reclaim.mjs`, the VS Code extension's Design Board quick-pick, its evidence refresh, `fleetController`, and `readinessController`. `scripts/stranded-report.mjs` and the extension's own board tree (`refreshBoard`/`boardViewProvider`) genuinely read `claim_window`/`scope`/`acceptance` and stay at the default. Narrows `gaps.d/task-query-board-has-no-response-size-bound.md`.
