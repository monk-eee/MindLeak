- **Bridge now has a dedicated Telemetry view at `/telemetry`.** Operators can
  look up a repository and see per-tool/transport/directive/storage/projection
  health, with current status (healthy/failing) derived from the most recent
  success or error, distinct from lifetime call and error counts.
