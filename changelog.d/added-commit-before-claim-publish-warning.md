- `canonical-push.mjs` now warns, advisory only and never blocking, when a branch being published carries a commit whose own timestamp predates every claim this session holds — `check_conformance` can never certify that commit's evidence no matter how the bundle is built afterward. See gaps.d/commit-then-claim-puts-evidence-before-its-claim.md, which the underlying limitation still leaves open.
