- **Ackplane now persists bounded human delegation grants as immutable events
  with a checked current projection.** The foundation records only routine
  authority envelopes and explicit revocations; it does not add a browser
  delegation command or a privileged fallback path.
