- Supervisor outboxes now retain original acknowledged lifecycle receipts and their sequence numbers atomically with delivery pruning, with an identity-bound, paginated library read for recovery inspection. Archive failures preserve pending frames and acknowledgement positions. This does not implement restart recovery, authorize work, or reconstruct receipts pruned by older builds.
