- **Exact publication evidence:** publishing a branch now records the published
  commit's own Git paths, full message and original timestamp through the same
  reader as the post-commit hook. Earlier branch changes, later `HEAD` changes
  and publication time no longer become false facts about that commit. An
  unreadable commit warns without inventing mutation evidence.
