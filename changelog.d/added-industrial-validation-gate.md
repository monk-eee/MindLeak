- Added a fail-fast industrial validation command (`make industrial-test`) and
  matching CI job that require explicit test and recovery databases, check
  PostgreSQL recovery tools, migrate before testing, and compile and test the
  workspace with all features. Added the industrial stabilization roadmap with
  task-level acceptance criteria.
