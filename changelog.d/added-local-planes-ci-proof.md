### Added

- CI now proves that the repository-local Rust planes and VS Code extension
  unit suite run natively without container, PostgreSQL, or package-network
  access after dependencies are installed.
