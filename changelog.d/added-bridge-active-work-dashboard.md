- **Bridge Agents gets its own dashboard, not just a section on Fleet.** `/agents` is a standalone page reading the existing `GET /api/v1/agents` contract (unchanged): repository and owner filters, the same allow-listed sort (`lease_expires_at`/`repository_id`/`owner_id`, ascending or descending) `parse_agents_sort` already validates, server-side paging, and an `aria-live` status region so loading/empty/error states are announced to assistive tech. It refreshes on the same bounded, visible-only cadence as Fleet's own sections (`startVisibleRefresh`) — no polling while the tab is hidden. The Fleet page's existing Agents section now links to it ("Open full dashboard"), and every Bridge page's shared nav gained a real `Agents` entry, kept honest by `nav_consistency.rs`'s self-maintaining guard. No new command, mutation, or control surface — this is a read-only view over data the fleet already exposes.
