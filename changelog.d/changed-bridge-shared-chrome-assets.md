- **Bridge's brand mark and grouped navigation are now one shared asset, not twelve copies.** `static/shared/chrome.css`/`chrome.js` (ADR-0124) are the single declared source for every page's brand mark and nav; each page now renders it into two small mount points instead of inlining its own copy of the markup, CSS, and disclosure script. A missing brand mark on five pages and three duplicated CSS dialects are both fixed as part of this change, and a future capability change is now a one-file edit instead of a twelve-file one.
