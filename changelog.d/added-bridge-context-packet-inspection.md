- **Bridge now exposes tenant-scoped Context Packet inspection.** Operators can
  page through bounded packet metadata and inspect use receipts without
  exposing compiled packet payloads, selections, exclusions, or local data.
