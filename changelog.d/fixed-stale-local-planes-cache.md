- **The `local-planes` CI job's Cargo cache was serving stale, pre-built test binaries.** Confirmed against PR #484: its own `local-planes` run passed without ever executing the test that PR added, because the restored cache's incremental build state did not reflect the checked-out commit. Bumped that job's `Swatinem/rust-cache@v2` `prefix-key` to start a fresh, correctly-keyed cache; other jobs are unaffected.
