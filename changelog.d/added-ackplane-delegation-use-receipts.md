- **Ackplane delegation uses now receive durable authorization receipts.** A named routine use is re-checked against the live human delegation's scope, delegatee session, policy and Constitution basis, action allow-list, effectivity, revocation state, and aggregate action/token limits. Bridge exposes a bounded read-only receipt page without revealing idempotency keys or payload digests.
