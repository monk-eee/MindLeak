- **`ackplane-server`'s `KnowledgeStore` is split below the module-length ratchet.** `knowledge_store.rs` (connection setup, query, record, retire) is now `knowledge_store.rs` (shared types plus `retire`) with `connection.rs`, `query.rs`, and `record.rs` siblings, alongside the existing `reach.rs`/`reconfirmation.rs`. `repository_id_guard.rs`'s structural tenant-scope guard now scans all five `impl KnowledgeStore` sources, not just one, so the split does not silently narrow ADR-0098 decision 5's coverage. No behavior change.
