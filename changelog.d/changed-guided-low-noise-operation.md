- **Live telemetry now changes behaviour instead of merely recording it.** `telemetry_snapshot` bundles graph health with a deterministic usage retrospective, separates degraded optional work from failures, and recommends `check_overlap` when writing sessions skipped memory. The VS Code Telemetry pane consumes one response on a 30-second visible/live cadence (instead of `graph_stats` plus telemetry every 3 seconds), Fleet defaults to 30 seconds, idle autonomous-index passes are silent, and an unavailable embedding service records one degraded transition before bounded backoff rather than repeating errors. Lodestar `open_session` and `lodestar_stats` now explain a zero-goal repository with concrete goal/import actions, while `advise` pushes up to three node-matched active lessons inline.
