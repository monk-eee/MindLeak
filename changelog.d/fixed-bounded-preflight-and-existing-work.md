- **`check_overlap`, `task_query(view="existing_work")`, and `task_create` no longer spill unbounded arrays.** `check_overlap` caps agent footprints strongest-first and reports `footprint_total`; `existing_work` and `task_create`'s `prior_work` cap their task lists to a recency-ordered preview while still reporting the exact full `count`/`already_done`/`already_serving_this_goal` and a `*_truncated` flag.
