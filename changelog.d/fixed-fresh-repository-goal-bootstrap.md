- **Fresh repositories can establish their first Lodestar goal through the
  default MCP surface.** The existing constitution create/query vocabulary is
  now discoverable before any goal exists, `advise` identifies that state as
  `no_constitution_adopted`, and setup guidance no longer assumes a commit or an
  `origin/main` remote.
