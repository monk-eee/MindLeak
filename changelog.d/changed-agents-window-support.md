- **The VS Code Agents window now has an explicit MindLeak support contract.**
  Local-folder Copilot CLI sessions can use both local stdio planes after the
  preview extension opt-in, while documentation now names the server/tool check,
  folder and worktree isolation, the explicit CLI-config fallback, and the
  boundaries for cloud, remote, and repository-less sessions.
