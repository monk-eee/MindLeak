- `ClaimDelegationService` authentication now binds the signed bytes to the exact RPC (`delegate`/`renew`/`release`/`recover`) and its own fields (branch, lease, paths, symbols, expected owner, reason) via `ackplane_protocol::claim_auth::ClaimOperation`, so a captured signature no longer verifies for a different operation or changed payload. This closes the remaining half of gaps.d/claim-authentication-can-be-replayed-across-operations.md, and the fragment is removed because a gap left open after it is fixed sends the next reader to rebuild finished work.
