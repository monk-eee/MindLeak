- **Ackplane gains an Industrial Work namespace (ADR-0120).** A repository may publish a bounded task projection -- title, acceptance, goal, lifecycle state, allocation, and lease -- distinct from `delegated_claims`' lease-only record and never a mirror of Local Lodestar's `spec.db`. The Bridge's Work view lists and inspects these tasks with the same tenant/repository authorization every other Bridge read enforces. This first slice is read-only (`WorkStore::create_task` is the seed/ingestion path; no Bridge mutation endpoint exists yet, per decision 8).
