### Added

- Added proposed ADR-0119, defining Industrial administration as scoped, receipted lifecycle operations rather than a generic reset. The policy distinguishes status, verified snapshots, redacted exports, recovery inspection, and retention-authorized purge while refusing browser SQL, raw database downloads, and privileged fallback without an explicit authorization basis.
