- Added `docs/INDUSTRIAL-QUICKSTART.md`: a worked path from a clean checkout to a running Ackplane server, an enrolled node, and a live supervisor or MCP client -- bringing up the Compose stack, trusting its development TLS certificate, matching the Bridge's derived tenant so an enrolled node is not silently invisible there, enrolling a node with `register-me`, and wiring `ackplane-supervisor`, `ackplane-mcp`, or `lodestar-mcp`'s federation client against it. This was previously scattered across `docker-compose.yml` comments, `crates/ackplane-supervisor/README.md`, and several ADRs with no single guide connecting them.
