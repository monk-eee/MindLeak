- **Fixed: `DesignStore`/`MaterializationStore` failed to connect against a genuinely fresh Postgres database.** Both only applied their own migration key, never the `evidence_records`, `constitution_publications`, or `work_tasks` migrations their own foreign keys depend on -- a long-lived shared development database already had all three from unrelated stores, hiding the gap until tested against a truly empty instance. Both now apply every migration they depend on before their own.
