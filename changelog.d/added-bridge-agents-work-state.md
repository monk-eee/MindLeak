- **Bridge Agents now shows each claim's native Work state and every tenant's unresolved waits.** `GET /api/v1/agents` marks a claim `has_native_work_task` (the cross-repository analogue of the per-repository `/work` claims-only check) and returns a bounded, tenant-wide `unresolved_waits` list — the same Board Doctor `UnansweredWait` finding, read across every enrolled repository at once instead of one at a time.
