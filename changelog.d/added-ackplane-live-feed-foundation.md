### Added

- Ackplane now has a durable, tenant-scoped Live Feed event foundation with
  opaque replay cursors, bounded delivery reads, typed resynchronization for
  unknown cursors, and retention pruning support for the future Bridge SSE
  operations stream.
