- **Added:** Work commands (ADR-0125 slice 3) can now be confirmed and executed server-side -- `CreateWork`/`RouteWork`/`ReleaseLease`/`AnswerWait`/`SubmitReview` each apply their Work/Claim mutation and commit the resulting receipt atomically in one transaction, idempotent on the command id, with typed (never panicking) outcomes for a stale version, an already-answered wait, or a changed claim owner/lease.
