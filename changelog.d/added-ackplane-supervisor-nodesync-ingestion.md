- **Enrolled supervisors can now publish typed runtime facts through NodeSync.**
  Registration, heartbeat, worker-session, and lifecycle frames are bound to
  the authenticated node connection and return durable typed acknowledgements.
