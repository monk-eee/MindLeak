- **The Bridge Fleet page now has the same navigation bar as Evidence and
  Telemetry.** `/` previously had no way to reach `/evidence` or `/telemetry`
  short of typing the URL; it now shares the identical `<nav>` (Fleet,
  Evidence, Telemetry live; Work, Design, Constitution, Knowledge shown as
  not-yet-available) so the three existing Bridge surfaces are mutually
  discoverable.
