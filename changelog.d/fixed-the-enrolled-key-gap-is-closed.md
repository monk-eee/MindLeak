- **The enrolled-key gap is closed.** `signing_keys` (migration `0004`) resolves an `EventEnvelope.signing_key_id` to the key that signed it, carrying activation, expiry, rotation overlap and revocation as history rather than corrections, and `service.rs` verifies a signature against it before the ledger appends. The `gaps.d` fragment that called the registry missing outlived its own fix and is removed, because a gap left open after it is fixed sends the next reader to rebuild finished work.
