- **Splitting a bound module strands its goal binding and orphans every descendant — two instances repaired and the recurrence now DETECTED 2026-08-29, but rebinding is still manual so this stays OPEN.** Goal↔code bindings name a path. When a bound file `X.rs` is split into `X/mod.rs` plus siblings, the binding keeps naming `X.rs`, which no longer exists: `scripts/binding-audit.mjs` then reports one stale binding *and* every new descendant as unbound. Measured 2026-08-29 at 36 unbound source files and 2 stale bindings. Both stale ones were exactly this: `crates/ackplane-bridge/src/administration.rs` split into `administration/{mod,export,policy,purge,recovery,snapshot}.rs` (commit `1db004ab`), leaving all six unbound; and `crates/ackplane-server/src/work_command_store/model.rs` split into `model/{mod,digest,validation}.rs`, whose descendants had since been bound separately, leaving only an obsolete entry. Repaired by binding the six descendants to the goal their predecessor held and unbinding both dead paths — 0 stale bindings, 30 unbound. **Why it recurs:** the `rust-module-length` control actively tells agents to split any module over 450 non-test lines, and 8 modules are over it right now, so the repository's own guidance drives the exact edit that breaks binding. The two controls are in tension and nothing connected them. **Now closed for detection:** `splitInto` recognises a vanished binding whose path became a same-named module directory holding Rust sources, and the audit reports it as `SPLIT` — listing the descendants to rebind and failing `--check`, rather than reporting it as a plain deletion whose obvious remedy (unbind) is the opposite of the right one. Verified against this session's own `daemon.rs` → `daemon/{mod,frames}.rs` split. **Still open:** the rebinding itself is manual, and the remaining 30 unbound files are mostly genuinely-new code whose governing goal is a judgement call, deliberately reported rather than mass-bound on a guess — binding a file to a too-broad goal makes conformance answer "covered" when a narrower clause should have governed, which is worse than being visibly unbound.
- **`binding-audit` was checkout-relative while the ledger is repository-shared, so "stale" depended on which worktree you ran it from — FIXED 2026-08-29, with one narrow case left OPEN.** Bindings live in the per-repository `spec.db` that every linked worktree shares, but the audit resolved each bound path against the working tree it happened to run in. Measured 2026-08-29 against one unchanged database: run from a checkout at `origin/main` it reported **4 stale bindings**; run from the worktree holding the unmerged branch that adds those files, **0**. The four were `crates/ackplane-mcp/src/{main,server,tools,endpoint}.rs`, correctly bound for a branch that had not landed yet. Impact was worse than noise: in a fleet (22 worktrees attached here) every binding for in-flight work read as stale from every other worktree, and the obvious remediation — unbind what the audit calls stale — would silently strip governance from another agent's unmerged code, which then lands ungoverned and invisible to conformance. `pathHolder` now checks the local tree first and then every remote branch, so a binding is stale only when no ref that could still land holds it; one still present elsewhere is reported as `IN FLIGHT`, naming the ref that keeps it alive, and does not fail `--check`. **Residual, minor:** a binding held only by a branch that is later abandoned stays reported as in-flight rather than stale until that branch is deleted, which is the safe direction to be wrong in.
