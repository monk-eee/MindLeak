- **High-cardinality read tools can still exceed the client result budget.** Observed in the live MCP surface: MindLeak `check_overlap` returned 43 KB for seven paths, Lodestar `task_query(view="existing_work")` returned 65 KB, and `task_create` returned 38 KB because they include full footprint, impact, or prior-work arrays. VS Code writes those results to `chat-session-resources` before an agent can inspect them, adding a disk round trip and context churn to ordinary preflight work. This run bounded conformance checks, completion responses, and completion offers; bounded summaries plus explicit pagination/detail reads for these other tools are left for later.
