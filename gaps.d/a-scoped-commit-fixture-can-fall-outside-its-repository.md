- **The scoped-commit adoption fixture can intermittently execute `git diff --cached` outside the temporary repository it created.** Observed in `editors/vscode/scripts/scoped-commit.test.mjs` test `allows a deliberate handover with --adopt-worktree`: the first full Vitest run failed with `git diff --no-index ... error: unknown option 'cached'`, while an immediate focused rerun of that file passed and the next full suite passed. Impact: a shared-host timing or fixture-isolation race can make the complete extension gate fail for code that did not touch scoped-commit, reducing trust in one-shot validation. Left unfixed this run because the operational-transparency task does not claim the commit-script implementation or fixture; reproduce under concurrency and make each fixture prove its Git working directory before invoking the script.
