- **`gaps --triage` counts a historical task reference as live remediation -- MEASURED 2026-08-26, OPEN.** `scripts/gaps.mjs` classifies a fragment as `task=yes` whenever its prose contains any `task:<12-hex>` token, then reports it as "tracked by a task" and excludes it from the orphaned backlog. The current catalog demonstrates the false green: `a-task-claimed-across-a-constitution-amendment-cannot.md` names `task:7b6154f1d69a`, `an-agent-can-work-all-day-certify-nothing.md` names `task:680b14565a8f`, and `update-branch-can-silently-drop-a-conflicts-losing-side.md` names `task:3780fd036ae6`; each task's Lodestar thread says it was accepted to `done`, while its gap fragment remains open. Impact: the reported 24 orphaned gaps is an understated count, and `board-health` can omit gaps that no active task is actually fixing. Left for later: make tracking explicit and verify that the referenced task exists and is nonterminal rather than treating incident prose as remediation metadata.
