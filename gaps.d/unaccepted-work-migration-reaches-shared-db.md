- **An unaccepted Work-domain migration can reach the shared development PostgreSQL database — OPEN.** Observed on 2026-08-22 while attempting the initial Work/Board Doctor foundation: running the branch-local `ackplane-migrate` path created `work_tasks`, `work_task_history`, `work_task_waits`, and `work_task_checkpoints` in the shared `ackplane-postgres-1` instance even though [ADR-0120](../docs/adr/0120-industrial-work-domain-is-an-authoritative-task-projection.md) remains Proposed. Impact: a shared development schema can get ahead of accepted integration code, obscuring migration identity and creating reconciliation risk across concurrent branches. Fixed this run: removed the uncommitted Work source, migration, and migrator wiring, then paused `task:8667a136d76a`; the shared tables were deliberately left untouched because they may be used by concurrent Work-domain drafting. Left for later: a reviewed policy and mechanism for isolating branch-local schema experiments or gating schema application on accepted authority.
