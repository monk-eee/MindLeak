- **`go.mod` indirect requirements are persisted as direct dependency edges -- MEASURED 2026-08-26, OPEN.** `crates/mindleak-core/src/ingest/manifest.rs` strips `//` comments before parsing each Go `require` line, discarding Go's `// indirect` marker and inserting the package like a direct dependency. This contradicts `gaps.d/manifest-dependency-support-is-direct-only.md`, which says transitive dependencies do not emit `depends_on` edges. The current integration test `go_mod_requirements_ignore_replace_directives` even requires a `depends_on` edge for `golang.org/x/sync v0.12.0 // indirect`, so the test suite codifies the contract violation. Impact: dependency impact queries report transitive Go modules as direct project dependencies. Left for later: retain the marker long enough to exclude indirect requirements and replace the assertion with a regression proving they do not enter the graph.
