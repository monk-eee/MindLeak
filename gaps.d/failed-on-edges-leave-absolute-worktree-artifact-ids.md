- **`failed_on` ingestion leaves absolute traceback paths outside the repository identity model -- MEASURED 2026-08-26, OPEN.** `crates/mindleak-core/src/ingest/execution.rs` makes `changed_files` repository-relative before creating `modified` edges, but directly turns `parse_error_locations` results into `artifact:` ids for `failed_on` edges. A failing Python traceback such as `File "/home/agent/Repos/MindLeak/src/lib.rs", line 42` therefore creates `artifact:/home/agent/Repos/MindLeak/src/lib.rs` instead of the canonical `artifact:src/lib.rs` that file ingestion creates. The existing `failed_execution_links_failed_on_edges_from_stack_trace` integration test covers only a relative path, while the facade explicitly refuses unplaceable absolute file-ingest paths to prevent this exact split. Impact: failure evidence fragments a real source file into worktree-local nodes, so impact and prior-failure queries miss the canonical artifact. Left for later: canonicalize error locations against known worktree roots and reject paths that remain absolute, with absolute Python and compiler traceback regressions.
