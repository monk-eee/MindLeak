- **Session-identity collapse can rewrite the owner of a paused task it must leave resumable -- MEASURED 2026-08-26, OPEN.** `crates/lodestar-core/src/db/repairs.rs` protects only a live `claimed` task from owner rewriting during identity collapse. `pause_task` keeps the owner while clearing its lease, and `resume_task` requires that exact owner string; a still-active legacy agent whose paused owner is collapsed cannot resume its own task and later risks another agent reclaiming it after the parking grace. This is a residual state omitted by the existing live-claim repair, whose invariant says ownership must not be changed outside audited transitions. Impact: an agent can lose its ability to continue or explain in-progress work without an ownership transfer or an evidence-window-preserving handoff. Left for later: exclude all owner-retaining nonterminal states from collapse and add paused and needs-input migration regressions.
