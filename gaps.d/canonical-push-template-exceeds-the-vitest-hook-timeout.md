- **The canonical-push test template can no longer finish within Vitest's hard 10-second hook timeout on a loaded Windows fleet host.** Observed twice consecutively in `editors/vscode/scripts/canonical-push.test.mjs` at `beforeAll`: one full-suite run and an immediate focused rerun both failed with `Hook timed out in 10000ms` while the hook initialized two Git repositories, configured identity, committed, and pushed the template. The test's own comment records a historical 4,019 ms setup measurement, so host contention can now consume the remaining margin. Impact: the complete extension suite can fail before any canonical-push assertion runs, masking unrelated changes and preventing a clean one-shot gate. Left unfixed this run because the operational-transparency task does not claim the publisher fixtures; measure the hook under fleet load, then either reduce its Git subprocess setup further or assign an explicit evidence-backed hook timeout.
