# MindLeak v0.1.4 release-candidate proof

- Status: preparation only; no `v0.1.4` tag or release exists
- Candidate payload SHA: pending final drained-main commit
- Candidate merge SHA: pending reviewed pull request
- Candidate tag/source SHA: pending separate maintainer release act
- Reconciled main SHA: `af60232a09379474bf65ec1f7e36250ce6cd0f99`
- Synchronized-main CI: [run 30538564943](https://github.com/monk-eee/MindLeak/actions/runs/30538564943) — passed

This record separates evidence available before publication from proof that can
exist only after a tag workflow. It must not be read as proof that public assets,
attestations, checksums, or a `v0.1.4` release already exist.

## Version parity

The prepared tree resolves every local Rust crate and the VS Code extension as
`0.1.4`:

- workspace package version: `0.1.4`;
- all six local packages in `Cargo.lock`: `0.1.4`;
- `editors/vscode/package.json`: `0.1.4`; and
- root and package entries in `editors/vscode/package-lock.json`: `0.1.4`.

The lockfile changes are local package-version changes only; no dependency was
upgraded as part of the release preparation.

## Local Windows rehearsal

A pre-candidate rehearsal was built from reconciled main
`bb269ca3ec263976b9ede3dfa1799021967fcd76` with the prepared 0.1.4 version
metadata. These hashes are rehearsal evidence and must be replaced after the
final candidate payload commit is created.

| Artifact | Size | Rehearsal SHA-256 |
|---|---:|---|
| `mindleak-v0.1.4-windows-x64.vsix` | 8,292,345 bytes | `fa971a9b185f2b5fece86c58019bf2152256156b47d7445609e2b89c0b20ec10` |
| `mindleak-v0.1.4-windows-x64.zip` | 7,702,823 bytes | `9976ffb2d20f8d49e55eb4cefd74b1f652822396d1c64dac05ebfe463b67d7e3` |

Verified locally:

- `cargo build --release --locked -p mindleak-mcp -p lodestar-mcp` completed;
- both native servers initialized over newline-delimited MCP, returned non-empty
  tool lists (27 MindLeak, 17 Lodestar), and resolved one shared session identity;
- the targeted `win32-x64` VSIX contained 32 runtime entries, both native
  servers, compiled extension output and media, with no source, tests, coverage,
  scripts or `node_modules` payload;
- the native ZIP contained exactly both servers, bundled `install.mjs`,
  `VERSION`, `LICENSE`, and `README.txt`;
- two independent native-ZIP builds had identical SHA-256 hashes; and
- the extracted archive installer completed in an isolated workspace, preserved
  binary hashes, generated VS Code and Copilot CLI registrations for both
  servers, and degraded optional semantic recall cleanly when no model was
  reachable.

The rehearsal exposed and fixed a release blocker: autonomous recall indexing is
now default-on and requires file-backed storage, while release and installer
smokes intentionally use `:memory:`. Both smoke environments now disable indexing
alongside pruning and consolidation. The focused installer regression was red
before the change and green afterward with 87.41% line / 80.5% branch coverage.

## Quality status

Completed on the prepared tree:

- Rust format and strict Clippy (`-D warnings`);
- full Rust workspace execution through Unit Test MCP (the adapter retains its
  known zero-count reporting limitation);
- extension ESLint and TypeScript compile;
- focused installer Vitest regression with coverage;
- documentation links after staging the changelog fold;
- ADR index, changelog validation, version parity, and whitespace checks; and
- real native-server, VSIX, native-ZIP, deterministic-package, and archive
  installer smokes described above.

The merge queue was drained before finalization. Synchronized-main CI passed on
`af60232a09379474bf65ec1f7e36250ce6cd0f99`, the exact main commit reconciled
into the release branch before the final changelog fold.

The full extension Vitest suite produced no assertion failure and fresh coverage
of 92.85% lines / 87.76% branches, but twice ended with the documented
`[vitest-worker]: Timeout calling "onTaskUpdate"` fleet-load error. Final
pull-request CI is the test-count authority and must be fully green before this
candidate can be accepted.

## Changelog and notes

The final candidate must be based on a drained merge queue. Every fragment on
that exact head is folded through
`node scripts/changelog.mjs --release 0.1.4`, leaving `Unreleased` empty and no
fragment files behind. `docs/RELEASE-NOTES.md` records the operator-visible
coordination, memory, packaging, reliability, compatibility, privacy, and known
limitation changes without claiming external adoption.

## Proof pending after review and tag

The following cannot be asserted before the separate release act:

- reviewed release-preparation PR and final merge SHA;
- synchronized-main CI on that merge SHA;
- immutable `v0.1.4` tag/source SHA;
- all four platform ZIP and VSIX assets;
- published `SHA256SUMS` matching downloaded assets;
- GitHub build-provenance attestations; and
- checksum-verified public install/runtime smoke.

After publication, replace the pending fields and rehearsal hashes with evidence
copied from the immutable release workflow and independently downloaded assets.
