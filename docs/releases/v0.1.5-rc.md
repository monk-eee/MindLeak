# MindLeak v0.1.5 release proof

- Status: published and independently verified
- Frozen source SHA: `bde932221f79ac3f2482fbc13114d09dc554ae79`
- Release branch: `release/publish-v0.1.5`
- Candidate payload SHA: `4b76b5f935906f660b12af8043aad10db46ec004`
- Candidate pull request: [#384](https://github.com/monk-eee/MindLeak/pull/384)
- Reviewed merge and final tag/source SHA: `6a3608895fb0b7510ebf3b6384ea2d438fb162cd`
- Public release: [MindLeak v0.1.5](https://github.com/monk-eee/MindLeak/releases/tag/v0.1.5)
- Release workflow: [run 30778087118](https://github.com/monk-eee/MindLeak/actions/runs/30778087118) - passed

This record keeps local candidate evidence separate from public proof. Public
claims below come from freshly downloaded release assets and strict attestation
verification, not from the workflow's intent or the locally built candidate.

## Frozen payload

The release boundary was taken from a drained `origin/main`: no pull request was
open when `bde932221f79ac3f2482fbc13114d09dc554ae79` was selected. The complete
delta from immutable `v0.1.4` contains fourteen substantive commits and fourteen
matching changelog fragments. The reviewed release preparation folded every
fragment and left `Unreleased` empty before tagging.

## Version parity

The tagged tree resolves every local Rust crate and the VS Code extension as
`0.1.5`:

- workspace package version and all seven local `Cargo.lock` packages;
- `editors/vscode/package.json`; and
- root and workspace entries in `editors/vscode/package-lock.json`.

The lockfile diff contains local package-version changes only. No third-party
dependency was upgraded by the release preparation.

## Local Windows candidate proof

Both native servers were built with `cargo build --release --locked` from
candidate `4b76b5f935906f660b12af8043aad10db46ec004`. They report
`0.1.5+4b76b5f93590`, initialize over newline-delimited MCP, return non-empty
tool lists (27 Memory Plane tools and 17 Intent Plane tools), and resolve one
shared registered session identity.

| Artifact | Size | Candidate SHA-256 |
|---|---:|---|
| `mindleak-v0.1.5-windows-x64.vsix` | 8,464,412 bytes | `23decceed7243546cb3dce74841cf04f98f4b910f2eff35d595fb10f8806eee4` |
| `mindleak-v0.1.5-windows-x64.zip` | 7,878,849 bytes | `1115f9b3664e1a554b7013531969e7423e3acbdfbdb79b287e35969b70f82aed` |

Verified locally:

- the targeted `win32-x64` VSIX contains 33 runtime entries, including compiled
  extension output and both native servers, with no source, tests, coverage,
  scripts, or `node_modules` payload;
- the native ZIP contains exactly 10 files: both servers, bundled `install.mjs`,
  the four canonical skill files, `VERSION`, `LICENSE`, and `README.txt`;
- the extracted installer succeeds in an isolated workspace, preserves both
  binary hashes, writes VS Code and Copilot CLI registrations, and installs
  `SKILL.md` plus setup, workflow, and troubleshooting references; and
- the focused installer suite passes at 88.55% line and 80.36% branch coverage.

The packaging rehearsal caught one release blocker before publication: the
skill linked `references/troubleshooting.md`, while the installer and tag
workflow originally carried only the other three skill files. Candidate
`4b76b5f935906f660b12af8043aad10db46ec004` requires, packages, installs, and
regression-tests the complete skill.

Repository-wide local validation also passed:

- Rust format and strict Clippy (`-D warnings`);
- the full Rust workspace through the Unit Test MCP custom runner;
- full extension Vitest coverage at 92.59% lines and 85.79% branches;
- extension TypeScript compile, ESLint, and Prettier;
- changelog, ADR index, relative links, version parity, diagnostics, and
  whitespace checks; and
- the pinned VS Code 1.101 Extension Host smoke with exit code 0.

Pull-request CI passed on Windows and Ubuntu Rust jobs, extension lint/compile,
coverage, and the pinned Extension Host smoke before the candidate was tagged.

## Public asset proof

The tag workflow ran once against
`6a3608895fb0b7510ebf3b6384ea2d438fb162cd`. Metadata validation, the quality
gate, all four native platform builds, and GitHub release publication completed
successfully. The public release contains exactly eight platform artifacts plus
`SHA256SUMS`:

| Public artifact | Bytes | SHA-256 |
|---|---:|---|
| `mindleak-v0.1.5-linux-x64.vsix` | 8,762,674 | `93aba1a7b5288c55b1e61065aa0291d73aa1474ab601fbc2f4e1e1fd508d096f` |
| `mindleak-v0.1.5-linux-x64.zip` | 8,177,128 | `cf0a4dec903ec2d7faeee121b6bfbf4203d4ee4fd779ab0d4604d4c23d3f81d9` |
| `mindleak-v0.1.5-macos-arm64.vsix` | 8,018,597 | `8ff6c50be9b843b58e5a8b31408c32af6a1e0368901ce33887ffc3c6bae351ea` |
| `mindleak-v0.1.5-macos-arm64.zip` | 7,433,050 | `f2d3ba06726e90da5be2421a3eb4bef91b1464070ea06a4d9a2311319c90b39c` |
| `mindleak-v0.1.5-macos-x64.vsix` | 8,440,812 | `ab885b63f691b0463fefac7aa9617a9d1c51b5555a12569943364a9f0f33db36` |
| `mindleak-v0.1.5-macos-x64.zip` | 7,855,265 | `e0d31dbbb32088976585a79791af676fb057970768aa8616cfdba84b036477fc` |
| `mindleak-v0.1.5-windows-x64.vsix` | 8,461,551 | `bb11df4453079c948b01ec0d6ea5398767fe5d494da1ce01515723d80b6c3c9b` |
| `mindleak-v0.1.5-windows-x64.zip` | 7,875,988 | `9db876db82d90f9fdbff6384cf7d2ec2a1b356fcc908428e8cedf764e14be6e9` |

All nine release files were downloaded into a fresh directory. The eight names
in `SHA256SUMS` exactly matched the expected four ZIP/four VSIX inventory, and
every computed digest matched. Each VSIX contains 33 runtime entries, compiled
extension output, and both native servers. Each native ZIP contains exactly 10
entries: both servers, installer, four complete skill files, version, license,
and README.

## Public provenance proof

`gh attestation verify` succeeded for all eight ZIP/VSIX subjects with policy
enforcing:

- repository `monk-eee/MindLeak`;
- signer workflow `.github/workflows/release.yml`;
- source ref `refs/tags/v0.1.5`;
- source digest `6a3608895fb0b7510ebf3b6384ea2d438fb162cd`; and
- GitHub-hosted runners.

The signed certificate identifies workflow `Release binaries`, push trigger,
run `30778087118` attempt 1, the tag ref and source/build digest above, GitHub's
OIDC issuer, and a Rekor transparency-log timestamp.

## Public Windows runtime proof

The independently downloaded Windows ZIP was extracted and its bundled
installer run in a fresh workspace. It preserved both native binary hashes,
wrote VS Code and Copilot CLI registrations, and installed the complete skill.
Every link in installed `SKILL.md` resolves, including
`references/troubleshooting.md`.

The installed public binaries initialize over MCP, expose 27 Memory Plane tools
and 17 Intent Plane tools, resolve one shared session identity, and both report
build `0.1.5+6a3608895fb0`. This verifies the public runtime, not only archive
structure or workflow status.
